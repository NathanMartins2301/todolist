import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    marginTop: 50,
  },
  textTitle: {
    color: "#808080",
    fontWeight: "700",
    fontSize: 14,
    textAlign: "center",
    marginTop: 16,
  },
  textDescription: {
    fontWeight: "200",
    color: "#808080",
    fontSize: 14,
    textAlign: "center",
  },
});
